# STATE — 루프 실행 기록

이 파일이 **실행 간 유일한 기억**이다. 에이전트는 매번 빈 컨텍스트에서 시작하므로,
여기 적히지 않은 것은 다음 실행에서 존재하지 않는 것과 같다.

## 기록 위치가 둘인 이유

이 파일은 **PR 브랜치에만 커밋**된다. 따라서 PR을 만들지 않은 실행
("게이트 차단", "처리 대상 없음")은 여기 남길 방법이 없다.

| 기록처 | 대상 | 성격 |
|---|---|---|
| **런 로그 — 이슈 #128 코멘트** | **모든 실행** | 유일하게 빠짐없는 기록 |
| `STATE.md` (이 파일) | PR을 만든 실행만 | 저장소에 남는 누적 이력 |

지난 실행을 확인할 때는 **런 로그를 먼저 본다.** 이 파일은 요약본이다.

## 기록 규칙

- PR을 만든 실행은 이 파일에도 항목을 추가하고 **같은 PR 브랜치에 커밋**한다
- 최신 항목을 **맨 위**에 추가한다 (역순)
- 항목이 30개를 넘으면 오래된 것부터 `## 보관` 섹션으로 접는다
- 기존 항목을 수정하지 않는다. 추가만 한다

## 항목 형식

```markdown
### <YYYY-MM-DD HH:MM> — <결과>

- **회수:** 없음 | #NN 락 해제 (사유)
- **게이트:** 통과 | 차단 (열린 agent-loop PR #NN)
- **후보:** #NN, #NN (자동 처리 가능으로 판정된 이슈)
- **선택:** #NN — <고른 이유, PRIORITY 근거>
- **탈락:** #NN (사유), #NN (사유)
- **결과:** PR #NN 생성 | needs-human #NN | 처리 가능 이슈 없음 | 실패 (사유)
- **검증:** lint ✅ build ✅ | lint ❌ (에러 요약)
- **비고:** <다음 실행이 알아야 할 것>
```

`<결과>`는 다음 중 하나:

| 값 | 의미 |
|---|---|
| `PR 생성` | 정상 완료. 사람 리뷰 대기 |
| `게이트 차단` | 진행 중 작업이 있어 아무것도 하지 않음 |
| `처리 대상 없음` | 자동 처리 가능한 이슈가 없음 (정상) |
| `에스컬레이션` | `needs-human`으로 넘김 |
| `실패` | 작업 중 문제 발생. 사유 필수 |

---

## 실행 기록

### 2026-08-03 12:26 — 에스컬레이션 (작업 완료, PR 생성 불가)

- **회수:** 없음 / 게이트: 통과 — 병합 대기 중인 `agent-loop` PR 없음
- **후보:** #153 (유일)
- **선택:** #153 — #152는 직전 실행에서 이미 처리해 브랜치 `chore/152-design-system-tune`
  (`41fa581`)에 작업이 올라가 있다. 라벨을 걸 권한이 없어 잠기지 않았지만,
  런 로그와 이 파일의 기록으로 중복 작업을 피했다
- **탈락:** #154(결정 문서) · #151(루프 구성 요소) · #135~#145(선행 블록) ·
  #134·#131·#146·#85·#54(`needs-human`) · #128·#102(`backlog`) · #133(완료, 종료 대상)
- **락:** **실패.** `gh issue edit --add-label in-progress`가 403
- **작업 내용:**
  - `app/layout.tsx`: `Space_Grotesk` → `Fraunces` (400/600/700, normal+italic).
    CSS 변수 `--font-display`는 유지
  - `styles/globals.css`: 헤딩 자간 `-0.01em` → `-0.005em`
- **판단 기록:** 완료 조건 3번이 자간을 "0 또는 -0.005em 범위로 완화"하라고 했다.
  **-0.005em**을 택했다 — 0으로 두면 세리프 제목이 느슨해 보이고, 기존 값에서
  절반만 줄이는 쪽이 변화 폭이 작다
- **검증:** lint ✅ build ✅ 폰트 경고 0건
  - Fraunces `@font-face`가 400/600/700 × normal/italic로 생성됨을 확인.
    **Space Grotesk는 빌드 결과에서 완전히 사라졌다**
  - 헤딩 규칙이 `font-family: var(--font-display), …, sans-serif; letter-spacing:-.005em`
    로 방출됨을 확인. **폴백에 `serif` 제네릭이 없다** (있으면 Windows에서 한글 제목이
    바탕체가 된다)
  - **중복 로드가 발생하지 않았다.** 굵기·스타일을 `resumeDoc.tsx`와 동일하게 맞춘
    결과 next/font가 두 인스턴스를 합쳐, `--font-fraunces`와 `--font-display`가
    같은 클래스(`__variable_0d86af`)를 공유한다. Fraunces 파일은 한 벌만 내려간다.
    (대조: JetBrains Mono는 굵기 집합이 달라 두 벌로 남아 있다 — #138에서 정리)
- **결과:** **PR을 열지 못했다.** `gh pr create`가 `must be a collaborator`로 거부.
  브랜치 `chore/153-fraunces`(`919ce70`)는 SSH로 푸시 완료
- **검사:** 수행하지 못했다 (8-1은 PR 번호를 전제로 함)
- **비고:** 이제 PR 없는 루프 브랜치가 **셋**이다 — `chore/151-loop-dev-sync`,
  `chore/152-design-system-tune`, `chore/153-fraunces`. 전부 lint·build를 통과했고
  각 이슈 코멘트에 PR 본문이 준비되어 있다. **토큰 권한이 복구되기 전까지 루프는
  같은 자리에서 계속 멈춘다** — 코드를 쓰고 푸시까지 하고 마지막에 막힌다

### 2026-07-31 15:44 — PR 생성

- **회수:** 없음 (`loop-reclaim.sh`: 워크트리 0개 회수. 열린 PR이 달린 브랜치
  `chore/132-design-tokens`·`chore/148-gate-park-needs-human`는 유지 /
  `loop-guard.sh --reap`: 락 걸린 이슈 없음)
- **게이트:** 통과 — PR #147이 병합되어(2026-07-31 06:13Z) 열린 `agent-loop` PR이
  없어졌다. PR #149는 열려 있으나 `agent-loop` 라벨이 없어 게이트 대상이 아니다
- **후보:** #133 (유일)
- **선택:** #133 — 후보가 하나뿐
- **탈락:**
  - #135·#136·#137·#138·#139·#140·#141·#142·#143·#144·#145 (선행 이슈 명시 =
    블록 상태 → 즉시 제외. #134가 열려 있는 한 #135·#136·#143은 계속 블록된다)
  - #134·#131·#146·#85 (이미 `needs-human`), #128·#102 (`backlog`) → 즉시 제외
  - #148 (연결된 열린 PR #149 존재 → 즉시 제외)
  - #54 → **`needs-human` 신규 부여.** 코드 쪽은 #109·#117·#119로 이미 끝났고
    남은 것이 Upstash 인스턴스 생성·Vercel 환경변수 주입·Firewall 룰뿐이다.
    저장소에서 할 일이 없으므로 루프가 집을 수 없다 (지난 실행은 이 이슈를
    코멘트 없이 제외했는데, TRIAGE 2관문은 라벨+사유 코멘트를 요구하므로 이번에
    바로잡았다)
- **정리 작업:** #132를 **수동 종료**했다. PR #147이 병합되어 완료 조건이 충족됐으나
  `dev` 대상 PR의 `Closes`는 기본 브랜치(`main`) 병합에서만 자동 발동하므로 열린
  상태로 남아 있었다. `origin/dev`에 토큰·유틸이 실제로 들어갔는지 확인한 뒤 닫았다.
  **닫지 않았다면 다음 실행이 이미 끝난 일을 다시 집었을 것이다**
- **결과:** PR #150 생성 → 검사 반려 → 에스컬레이션 → 사람 결정 후 재작업 (`chore/133-jetbrains-mono`)
- **검사 1회차:** `issue-reviewer` 판정 **반려.** `--font-mono`가 Tailwind 4 기본 테마
  변수와 이름이 겹치고 `font-mono` 유틸이 **25개 파일·58회** 사용 중이어서, 병합 시
  화면 작업 없이 전역 고정폭이 즉시 바뀐다는 지적. 직접 세어 확인했고 PR 본문의
  "쓰는 곳이 없다"는 서술이 틀렸음을 정정 코멘트로 남겼다
- **재작업하지 않고 에스컬레이션한 이유:** 수정 방법이 변수명 변경인데 이슈 완료 조건
  1번이 이름을 `--font-mono`로 지정한다. 완료 조건과 범위 밖 조항이 충돌하므로 8-2가
  아니라 9번으로 갔다
- **사람 결정 → 재작업:** `--font-jbmono`로 변경하는 방향을 선택했다. 4군데 수정
  (`app/layout.tsx`의 `variable`, 유틸 3개의 `var()`). 재검증에서 `--font-mono`가
  Tailwind 기본 정의만 남고 `.font-mono{font-family:var(--font-mono)}`가 원래 스택을
  가리키는 것을 확인 — 기존 58곳은 영향 없다. `components/resumeDoc.tsx`는 자기
  인스턴스를 `.resume-page` div에만 적용하므로 `/resume`도 동작 변화 없다
- **검증:** lint ✅ build ✅
  - 빌드 로그에 폰트 관련 경고 0건. 로그의 `Compiled with warnings`는 `next-auth`가
    쓰는 `jose`의 엣지 런타임 경고로 기존 것이다
  - JetBrains Mono `@font-face`가 굵기 400·500·600으로 생성되는 것을 확인
  - 유틸 3개는 사용처가 없으면 방출되지 않으므로 임시 파일로 한 번 빌드해 출력을
    확인한 뒤 삭제. 최종 변경 파일은 2개
- **비고:** 다음 실행이 알아야 할 것 —
  1. **후보가 고갈된다.** #133이 병합되면 남은 리디자인 하위 이슈는 전부 #134 또는
     #135(상단 필 내비)를 선행으로 요구한다. #134는 `needs-human`이고 #135는 #134를
     선행으로 두므로, **사람이 #134를 처리하지 않으면 루프가 집을 이슈가 없다.**
     그 경우 `처리 대상 없음`이 정상 결과다
  2. **PR #148도 수동 종료가 필요할 수 있다.** PR #149(게이트 수정)가 `origin/dev`에
     병합됐으므로 이슈 #148은 #132와 같은 이유로 열린 채 남는다. 다음 실행은 반영을
     확인하고 닫아라
  3. 이번 실행에서 발견 — `components/resumeDoc.tsx`가 이미 JetBrains Mono를
     `--font-jbmono`로 로드하고 있었다. #133 본문의 "현재 없음"은 사실과 달랐다.
     PR #150 병합 후 같은 글꼴이 두 인스턴스로 로드된다. 범위 밖이라 손대지 않고
     PR 본문에 후속 정리 방법(#138에서 통합)을 적어 두었다

### 2026-07-31 11:33 — PR 생성

- **회수:** 없음 (`loop-reclaim.sh`: 루프 워크트리 0개, 회수 대상 브랜치 0개.
  `chore/122-issue-loop`·`feat/54-auth-ratelimit`·`feat/54-durable-ratelimit`는
  `agent-loop` 라벨이 없어 사람 브랜치로 판정되어 유지됨 /
  `loop-guard.sh --reap`: 락 걸린 이슈 없음)
- **게이트:** 통과 — 열린 `agent-loop` PR 없음
- **후보:** #132, #133
- **선택:** #132 — 두 후보 모두 `chore`(30점) 동점, 변경 범위도 2개 파일로 동점 →
  3순위 기준(이슈 번호가 작은 것)으로 #132. 리디자인 v2의 선행 단계이기도 함
- **탈락:**
  - #133 (후보였으나 PRIORITY 3순위에서 밀림. 라벨·조건 모두 자동 처리 가능하므로
    다음 실행의 유력 후보다)
  - #134 (다크 모드 제거. 변경 8개 파일이 `components/`·`app/`·`styles/`·저장소 루트에
    걸쳐 "3개 이상 디렉터리" 조항에 해당 → `needs-human` + 쪼개는 방법 3단계 제안 코멘트.
    의존성 제거와 lockfile 갱신이 포함된 점도 근거로 남겼다)
  - #135·#136·#137·#138·#139·#140·#141·#142·#143·#144·#145 (본문에 선행 이슈가
    명시된 블록 상태 → 즉시 제외, 코멘트 없음)
  - #131 (리디자인 v2 전체 추적. 이미 `needs-human`)
  - #146 (Notion 수치 필드 연동. 이미 `needs-human`)
  - #128 (`backlog`), #102 (`backlog`), #85 (`needs-human`) → 즉시 제외
  - #54 (봇/크롤러 방어. 라벨·열린 PR이 없어 즉시 제외 조건에는 걸리지 않으나,
    `lib/rateLimit.ts`와 Vercel 설정이 대상이라 TRIAGE "보안·자격증명" +
    "외부 서비스 계약 변경"에 해당. 이미 사람이 인프라 잔여만 남겨둔 이슈이므로
    중복 코멘트를 남기지 않고 후보에서만 제외했다)
- **결과:** PR #147 생성 — `chore/132-design-tokens`
- **검증:** lint ✅ build ✅
  - 빌드된 CSS에서 새 토큰 15개가 `:root`에 방출되는 것과 `.card`가
    `border-radius:26px`로 나오는 것을 확인
  - 새 유틸리티 5개는 사용처가 없으면 Tailwind가 방출하지 않으므로, 다섯 클래스를
    모두 쓰는 임시 파일로 한 번 빌드해 출력이 스펙과 일치함을 확인한 뒤 그 파일을
    삭제했다. 최종 변경 파일은 2개
- **비고:** 다음 실행이 알아야 할 것 —
  1. **런 로그 이슈 #128에 코멘트가 0건이었다.** 지난 실행(2026-07-30 16:40)이
     `STATE.md`에는 기록을 남겼지만 #128에는 남기지 않았다. 절차 11번이 빠졌던 것으로
     보인다. 이번 실행부터 다시 남긴다
  2. #132가 병합되면 #133이 유력 후보다. #135 이후는 전부 선행 이슈 대기 상태다
  3. #134에 `needs-human`을 새로 달았으므로 다음 실행에서는 조용히 걸러진다.
     #135·#136·#143이 #134를 선행으로 지정하고 있어 #134가 열려 있는 동안 이 셋은
     계속 블록 상태다 — 사람이 #134를 처리하지 않으면 리디자인 트랙이 #133 이후로
     진행되지 않는다
  4. PR #147은 처음에 다크 모드 본문 색 회귀를 남겼다(이슈가 `.dark` 블록 수정을
     범위 밖으로 지정했기 때문). 검사자가 이를 독립 확인해 `사람 필요`로 판정했고,
     **사람이 "`.dark`에 `--ink` 한 줄 추가"를 선택**해 이슈 #132의 범위를 그만큼
     완화했다. `.dark`의 `--text`를 `--ink`로 바꾸는 1줄 수정으로 회귀를 해소했고
     변경 파일 수는 2개 그대로다. 병합 순서 제약도 함께 사라졌다

### 2026-07-30 16:40 — PR 생성

- **회수:** 없음 (`loop-reclaim.sh`: 루프 워크트리 0개, 회수 대상 브랜치 0개 /
  `loop-guard.sh --reap`: 락 걸린 이슈 없음)
- **게이트:** 통과 — 열린 `agent-loop` PR 없음
- **후보:** #126 (유일)
- **선택:** #126 — 후보가 하나뿐. `priority` 라벨 없음 → 일반 경로(base `dev`)
- **탈락:**
  - #122 (루프 기반 구축 트래킹 이슈. 하위 체크박스 7개로 TRIAGE 범위 초과.
    런 로그 대상 이슈 자체이므로 `needs-human` 라벨은 달지 않고 런 로그에만 기록)
  - #121 (디자인 결정 + 토큰 의미 반전 → `needs-human`, 쪼개는 방법 제안 코멘트)
  - #120 (전면 개편, 3개 이상 디렉터리 → `needs-human`)
  - #102 (`backlog` 라벨 → 즉시 제외, 코멘트 없음)
  - #85 (Vercel 시크릿 + 콘텐츠 창작 → `needs-human`)
  - #54 (열린 PR #119 존재 → 즉시 제외, 코멘트 없음)
- **결과:** PR 생성 — `chore/126-output-file-tracing-root`
- **검증:** lint ✅ build ✅
  - 원본 config로는 워크스페이스 루트 추정 경고가 재현됨(저장소 루트의 추적되지 않은
    `pnpm-lock.yaml`을 루트로 선택). 수정 후 `lint`·`build` 양쪽에서 경고 0건
  - 빌드 라우트 표를 수정 전/후로 비교해 동일함을 확인 (27개 항목, 크기 수치 제외 후 diff 없음)
- **비고:** #121·#120·#85에 `needs-human`을 새로 달았다. 다음 실행에서는 이 3개가
  "즉시 제외"로 조용히 걸러지므로 후보 목록에 나타나지 않는다.
  자동 처리 가능한 이슈가 남지 않을 가능성이 높다 — 그 경우 `처리 대상 없음`이 정상이다.

### 2026-07-30 — 초기화

- **회수:** 해당 없음
- **게이트:** 해당 없음
- **후보:** 없음
- **선택:** 없음
- **결과:** 루프 기반 구축 (#122). 아직 루프를 실행하지 않음
- **검증:** 해당 없음
- **비고:** 첫 실행 전 확인 사항 —
  루프용 라벨(`agent-loop`, `in-progress`, `needs-human`)이 생성되어 있어야 한다.
  없으면 `gh label list`로 확인 후 만든다.

  현재 열린 이슈 5개(#121 #120 #102 #85 #54)는 TRIAGE 기준상 전부 자동 처리 대상이 아니다.
  따라서 **첫 실행의 기대 결과는 `처리 대상 없음`** 이다.
  이것이 정상 동작이며, 이 중 하나라도 집는다면 TRIAGE 기준이 느슨한 것이다.
